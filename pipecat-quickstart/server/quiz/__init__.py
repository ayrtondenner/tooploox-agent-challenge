"""Markdown-powered quiz agent — Tooploox code challenge.

Fetches a Markdown document from a URL, generates a multiple-choice quiz
with LangChain + OpenAI, runs it through a Gradio UI, and persists
answers + final score in SQLite.
"""
